{$SITE_ADDRESS} {
    root * /srv
    encode gzip

    handle /api/* {
        uri strip_prefix /api
        reverse_proxy {$BACKEND_UPSTREAM}
    }

    handle {
        try_files {path} /index.html
        file_server
    }
}
