name: keepalive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ping Render health endpoint
        run: curl -sf --max-time 30 https://moodly-oyew.onrender.com/api/health
